import pymongo, requests

connection = pymongo.MongoClient('mongo.getsporty.space')
db = connection.getsporty
spherical_distance = 750/1000

#db.sportsgrounds.createIndex({point:"2dsphere"})
for i in db.sportsgrounds.find({
   'location': {
     '$near': {
       '$geometry': {
          'type': "Point",
          'coordinates': [149.126482,-35.331553]
       },
       '$minDistance': 5,
       '$maxDistance': 1000,
     }
   }
}):
    print i['name']
